# SQL Migrations

This directory contains SQL migration scripts for the Scrapeyard database schema.

Migration scripts should be numbered sequentially (e.g., `001_create_jobs.sql`,
`002_create_errors.sql`) and applied in order during service initialization.
